<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:startapp="clr-namespace:StartApp" x:DataType="startapp:TabataExerciseControl"
             x:Class="StartApp.TabataExerciseControl">
  <ContentView.Content>
        <Frame
            CornerRadius="10"
            Margin="10"
            BackgroundColor="DarkOrange">
            
       
      <StackLayout
          HorizontalOptions="FillAndExpand"
          VerticalOptions="FillAndExpand"
          
          Padding="10">
            <Label 
                x:Name="Name"
                FontSize="Medium"
                TextColor="Black"></Label>
            <Label 
                x:Name="Sets"
                FontSize="Medium"
                
                TextColor="Black"></Label>
            <Label 
                x:Name="Cycles"
                FontSize="Medium"
                TextColor="Black"></Label>
            <Label 
                x:Name="RestBetweenSets"
                FontSize="Medium"
                
                TextColor="Black"></Label>
        </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>